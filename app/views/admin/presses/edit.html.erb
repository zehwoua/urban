<%= render 'admin/admin_sidebar' %>

<div class="mainbar">
	<%= render 'admin/admin_pagehead',:pagehead_title => "Edit Press : #{@press.title}" %>

  <div class="matter">
  	<div class="container-c">
      <%= link_to 'Back to Presses', admin_presses_path, :class => "btn btn-warning" %>
      <%= link_to 'New Press', new_admin_press_path, :class => "btn btn-success" %>

      <div class="clearfix"></div>
			<%= render 'form' %>
		</div><!--/container-c-->
	</div><!--/matter-->
</div>