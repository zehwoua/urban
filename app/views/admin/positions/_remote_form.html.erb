<%= simple_form_for [:admin, @position], :remote => true, :authenticity_token => true  do |f| %>
	<% if @position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% @position.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= f.input :name %>
	<%= f.button :submit,"Create Member Position", :class => "btn btn-gray btn-block" %>
<% end %>