<%= render 'admin/admin_sidebar' %>

<div class="mainbar">
	<%= render 'admin/admin_pagehead',:pagehead_title => "Edit Position : #{@position.name}" %>

  <div class="matter">
  	<div class="container-c">
      <%= link_to 'Back to Positions', admin_positions_path, :class => "btn btn-warning" %>
      <%= link_to 'New Position', new_admin_position_path, :class => "btn btn-success" %>

      <div class="clearfix"></div>
			<%= render 'form' %>
		</div><!--/container-c-->
	</div><!--/matter-->
</div>